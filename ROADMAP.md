# Enterprise Copilot Roadmap

本 Roadmap 以“先夯实服务基础，再交付可控的企业知识 Copilot”为原则。阶段顺序代表依赖关系，不承诺固定发布日期；每个里程碑均以可验证的退出标准作为完成条件。

## 当前基线：后端 MVP

- [x] FastAPI 服务入口与 OpenAPI 文档
- [x] 健康检查
- [x] 用户 API 原型与 Pydantic 校验
- [x] PostgreSQL、Redis、API 的 Docker Compose 编排
- [x] pytest 与 Ruff 基础配置
- [x] 修正 CI：在 `apps/web` 创建前跳过或移除前端任务

## M1：持久化与工程基线

目标：将演示 API 升级为可持续演进的数据服务。

- [ ] 引入 SQLAlchemy/SQLModel 与数据库迁移工具
- [ ] 用 PostgreSQL 替换进程内 `fake_users`
- [ ] 增加配置分层、启动检查和结构化日志
- [ ] 为分页参数增加边界校验和统一错误响应
- [ ] 增加单元测试、集成测试和覆盖率门槛
- [ ] 增加 `/ready` 就绪检查，验证关键依赖

退出标准：API 重启后数据不丢失；迁移可重复执行；CI 能独立拉起依赖并通过全部检查。

## M2：身份、租户与权限

目标：建立企业场景所需的安全边界。

- [ ] 支持 OIDC/OAuth 2.0 登录与令牌校验
- [ ] 建立组织、成员、角色和权限模型
- [ ] 实现租户级数据隔离
- [ ] 增加审计日志、敏感字段脱敏和密钥管理
- [ ] 为管理端接口补充限流与权限测试

退出标准：所有受保护接口均经过鉴权；跨租户访问测试可靠失败；关键操作可审计。

## M3：企业知识库与检索

目标：形成可追溯、可更新的企业知识底座。

- [ ] 建立文档上传、解析、切分和元数据模型
- [ ] 接入对象存储与向量检索组件
- [ ] 通过 Redis 承载异步任务状态或缓存
- [ ] 支持增量索引、删除传播与失败重试
- [ ] 实现混合检索、重排和引用溯源
- [ ] 建立离线检索评测集与质量指标

退出标准：文档从导入到可检索的链路可观测、可重试、可删除；检索结果能返回可靠来源。

## M4：Copilot 对话体验

目标：交付有权限约束和来源引用的问答闭环。

- [ ] 建立模型网关，统一超时、重试、配额和供应商切换
- [ ] 实现基于权限过滤的 RAG 对话链路
- [ ] 支持流式响应、会话历史和反馈采集
- [ ] 创建 Web 客户端并接通 CI 的前端任务
- [ ] 提供答案引用、无答案策略和提示注入防护
- [ ] 记录延迟、Token 成本、检索质量与用户反馈

退出标准：用户可在 Web 端基于授权文档完成带引用问答；关键质量、安全和成本指标可查看。

## M5：生产化与扩展

目标：达到可灰度发布、可运营和可恢复的生产标准。

- [ ] 增加 OpenTelemetry、指标、追踪、告警和仪表盘
- [ ] 建立镜像扫描、SBOM、依赖更新与安全测试
- [ ] 增加备份恢复、灾难恢复与容量压测
- [ ] 支持灰度发布、回滚和数据库迁移保护
- [ ] 建立提示词/模型版本管理与自动化评测门禁
- [ ] 完成运行手册、SLO 与事件响应流程

退出标准：发布可回滚、故障可定位、数据可恢复，并有明确的 SLO 和值班响应机制。

## 横向原则

- 安全：默认最小权限，敏感数据不进入日志或未经批准的模型上下文。
- 质量：功能变更必须有测试；AI 能力必须同时评估正确性、引用、安全与成本。
- 可观测性：关键请求贯穿请求 ID，后台任务和模型调用均可追踪。
- 可替换性：数据库、模型和检索实现通过清晰接口隔离，避免供应商锁定。
- 文档：架构决策、部署步骤、数据迁移和运维流程与代码同步更新。
